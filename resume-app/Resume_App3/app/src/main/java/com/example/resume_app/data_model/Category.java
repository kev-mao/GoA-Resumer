package com.example.resume_app.data_model;

/**
 * Because otherwise, we would be passing around Strings corresponding to each category,
 * which seems a little risky.
 */
public enum Category {
    AWARD,
    CERTIFICATION,
    EDUCATION,
    EXPERIENCE,
    SKILL
}
